import { TemplateDiagnostics } from './template'

/**
 * Describes a collection of diagnostics for a page organized by types
 * (object keys).
 */
export interface PageDiagnostics extends TemplateDiagnostics {}
