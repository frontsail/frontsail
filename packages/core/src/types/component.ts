import { TemplateDiagnostics } from './template'

/**
 * Describes a collection of diagnostics for a component organized by types
 * (object keys).
 */
export interface ComponentDiagnostics extends TemplateDiagnostics {}
